// Insertion sort in C++

#include <iostream>
using namespace std;
#include <cstdio>
#include<cstring>

int main ()
{
    string str("Hello");
    cout<<"Length = "<<str.length()<<" size = "<<str.size()<<endl;
//////////////////////////////////////////////////////////////////////////////////////////
     // char s1[5];
     // char s2[12] = "Hello World";

     // strncpy(s1,s2,5);
     // printf("String s1 is: %s", s1);
/////////////////////////////////////////////////////////////////////////////////////////
    // char fname[50];
    
    // char buffer[] = "Dennis Ritchie Hello World";
    // int length = sizeof(buffer);
    // int ptr=0;
    // while(ptr<length-1)
    // {
    //      int local = sscanf(buffer, "%s", fname);
    //     printf("local = %d\n",local);
    //     ptr = ptr + local;
    //    printf("First name : %s\n", fname);
        
    // }
    


    return 0;
}
